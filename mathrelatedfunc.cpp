#include <iostream>
#include <cmath>

int main () 
{
    double x = 3.99;
    double y = 4;
    double z;

    //z = std::max(x, y); //
    //z = std::min(x, y);
    //z = pow(2, 3);
    //z = sqrt(9);
    //z = abs(-3);
    //z = round(x); //rounds to the nearest integer
    //z = ceil(x); //rounds up to the nearest integer
    //z = floor(x); //rounds down to the nearest integer

    std::cout << z; 


    return 0;
}